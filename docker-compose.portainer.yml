services:
  planner:
    image: ghcr.io/andreab-1/projectplanner:latest
    ports:
      - "${PORT:-8080}:80"
    volumes:
      - planner_projects:/data/projects
    restart: unless-stopped

volumes:
  planner_projects:
